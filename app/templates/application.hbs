<table>
{{#each model as |book|}}
  <tr>
    <td>{{book.id}}</td>
    <td>{{book.title}}</td>
    <td>{{book.author}}</td>
    <td>{{book.year}}</td>
    <td>{{book.pageCount}}</td>
  </tr>
{{/each}}
</table>
